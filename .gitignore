.DS_Store
node_modules
.next
# Next.js auto-generates this on dev/build; tracking it makes the precommit's
# `next dev` (e2e) rewrite it and fail prek with "files were modified".
next-env.d.ts
storybook-static
coverage
playwright-report
test-results
.eve
.workflow-data
.output
.claude/*
!.claude/skills/
.env
.env.local
.env.*.local
# Filled-in copy of scripts/prod-env.example; holds live production secrets.
prod-env.local
CLAUDE.local.md
tsconfig.tsbuildinfo
.vercel
