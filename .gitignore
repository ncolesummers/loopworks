.DS_Store
node_modules
.next
# Next.js auto-generates this on dev/build; tracking it makes the precommit's
# `next dev` (e2e) rewrite it and fail prek with "files were modified".
next-env.d.ts
storybook-static
coverage
playwright-report
test-results
.eve
.workflow-data
.output
.claude/*
!.claude/skills/
.env
.env.local
.env.*.local
# Filled-in copies of scripts/vercel-env.example; hold live environment secrets.
vercel-env.local
vercel-env.*.local
prod-env.local
CLAUDE.local.md
tsconfig.tsbuildinfo
.vercel

# Agent browser screenshots land here when a shell cwd resets; never commit them.
/*.png

# Polly orchestrator scratch; agents must not delete or clean this directory.
.polly/
